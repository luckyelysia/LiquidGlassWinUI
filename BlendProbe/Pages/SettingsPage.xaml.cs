using Microsoft.UI.Xaml.Controls;

namespace BlendProbe.Pages
{
    // Settings page for the NavigationView shell. Placeholder controls; wire up
    // real settings once the app grows.
    public sealed partial class SettingsPage : Page
    {
        public SettingsPage()
        {
            InitializeComponent();
        }
    }
}
